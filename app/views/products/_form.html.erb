<%= form_with(model: product, local: true) do |form| %>
  <% if product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <table>
     <tr>

       <td> <%= form.label :category %> </td>
       <td><%= select_tag "categories", options_from_collection_for_select(Cate.all, "id", "productname") %>
       </td>



  <tr>
     <td><%= form.label :name %></td>
     <td><%= form.text_field :name %></td>
  </tr>

  <tr>
    <td><%= form.label :image %></td>
    <td><%= form.file_field :image %></td>
  </tr>

   <tr>
    <td><%= form.label :description %></td>
    <td><%= form.text_field :description %></td>
  </tr>

<tr>
  <td> <%= form.label :dat %></td>
  <td><%= form.date_field :dat %></td>
</tr>

<tr>
  <td>  <%= form.submit %></td>
</tr>
<% end %>
